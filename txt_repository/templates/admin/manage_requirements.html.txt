{% extends "admin/admin_base.html" %}
{% block title %}Manage Requirements - AYUDA{% endblock %}

{% block extra_css %}
<style>
    :root {
        --req-blue: #1f6feb;
        --req-indigo: #1942a8;
        --req-cyan: #0ea5a6;
        --req-surface: #ffffff;
        --req-border: #dbe6fb;
        --req-text: #1f2937;
        --req-muted: #64748b;
    }

    .requirements-shell {
        animation: requirementsFadeIn 0.35s ease-out;
    }

    @keyframes requirementsFadeIn {
        from {
            opacity: 0;
            transform: translateY(8px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .requirements-hero {
        background:
            radial-gradient(circle at top right, rgba(14, 165, 166, 0.17), transparent 42%),
            linear-gradient(132deg, #f6f9ff 0%, #ffffff 62%);
        border: 1px solid var(--req-border);
        border-radius: 16px;
        box-shadow: 0 10px 24px rgba(31, 42, 68, 0.08);
        padding: 1.25rem 1.4rem;
    }

    .requirements-hero h1 {
        color: #102a52;
        font-weight: 700;
        letter-spacing: 0.2px;
    }

    .requirements-hero .subtitle {
        color: var(--req-muted);
        margin-bottom: 0.55rem;
    }

    .hero-kpis {
        display: flex;
        flex-wrap: wrap;
        gap: 0.45rem;
    }

    .hero-kpis .kpi-chip {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        padding: 0.25rem 0.65rem;
        border-radius: 999px;
        font-size: 0.74rem;
        font-weight: 700;
        letter-spacing: 0.25px;
        text-transform: uppercase;
        border: 1px solid #d7e5fd;
        background: #f5f9ff;
        color: #21406f;
    }

    .hero-actions .btn {
        border-radius: 10px;
        font-weight: 600;
        padding: 0.58rem 1.05rem;
    }

    .hero-actions .btn-primary {
        background: linear-gradient(135deg, var(--req-blue) 0%, var(--req-indigo) 100%);
        border: none;
    }

    .hero-actions .btn-primary:hover {
        transform: translateY(-1px);
    }

    .filter-section {
        background: var(--req-surface);
        border: 1px solid var(--req-border);
        border-radius: 14px;
        box-shadow: 0 8px 22px rgba(30, 45, 70, 0.06);
        padding: 1rem 1.1rem;
        margin-bottom: 1.4rem;
    }

    .filter-label {
        display: block;
        margin-bottom: 0.35rem;
        font-size: 0.76rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        font-weight: 700;
        color: #3e5d8c;
    }

    .filter-section .form-control,
    .filter-section .input-group-text {
        border-color: #d4e2fa;
        border-radius: 10px;
    }

    .filter-section .form-control:focus {
        border-color: #7aa8f5;
        box-shadow: 0 0 0 0.15rem rgba(31, 111, 235, 0.14);
    }

    .filter-clear-btn {
        border-radius: 10px;
        font-weight: 600;
    }

    .section-surface-card {
        border: 1px solid #dce8fb;
        border-radius: 14px;
        box-shadow: 0 10px 26px rgba(30, 45, 70, 0.07);
        overflow: hidden;
    }

    .section-header {
        border-bottom: 1px solid #d9e6fb;
        padding: 0.85rem 1rem;
    }

    .section-header.doc-header {
        background: linear-gradient(135deg, #1f6feb 0%, #1942a8 100%);
        color: #fff;
    }

    .section-header.qual-header {
        background: linear-gradient(135deg, #16945a 0%, #107649 100%);
        color: #fff;
    }

    .section-add-btn {
        border-radius: 9px;
        font-weight: 600;
    }

    .requirement-card {
        border: 1px solid #e3eaf8;
        border-radius: 12px;
        background: #fff;
        transition: transform 0.22s ease, box-shadow 0.22s ease, border-color 0.22s ease;
        box-shadow: 0 4px 12px rgba(22, 36, 63, 0.05);
        overflow: hidden;
    }

    .requirement-card:hover {
        border-color: #bfd5f7;
        transform: translateY(-3px);
        box-shadow: 0 14px 24px rgba(27, 50, 87, 0.12);
    }

    .requirement-header {
        cursor: pointer;
        padding: 0.95rem 1rem;
        border-bottom: 1px solid #e7effb;
        background: linear-gradient(180deg, #fbfdff 0%, #f6f9ff 100%);
    }

    .requirement-header:hover {
        background: linear-gradient(180deg, #f4f8ff 0%, #eef4ff 100%);
    }

    .requirement-name {
        display: flex;
        align-items: center;
        gap: 0.35rem;
        font-weight: 700;
        color: var(--req-text);
        margin-bottom: 0.55rem;
        line-height: 1.3;
    }

    .expand-cue {
        color: #7b92b4;
        font-size: 0.78rem;
        margin-right: 0.35rem;
    }

    .requirement-body {
        padding: 1rem;
        background: #fff;
    }

    .usage-badge {
        font-size: 0.72rem;
        padding: 0.3rem 0.55rem;
        border-radius: 999px;
        font-weight: 600;
    }

    .program-tag {
        background: #e8f1ff;
        color: #24579b;
        padding: 0.26rem 0.6rem;
        border-radius: 999px;
        font-size: 0.74rem;
        margin: 0.12rem;
        display: inline-block;
        border: 1px solid #d7e4fb;
    }

    .program-tag.mandatory {
        background: #fff5df;
        color: #a36a00;
        border-color: #f6deaa;
    }

    .action-buttons {
        opacity: 0;
        transition: opacity 0.2s ease;
        display: inline-flex;
        align-items: center;
    }

    .requirement-card:hover .action-buttons {
        opacity: 1;
    }

    .action-buttons .btn {
        border-radius: 8px;
        min-width: 2rem;
    }

    .requirement-description {
        color: #5f6f86;
        font-size: 0.87rem;
        margin-bottom: 0.75rem;
        line-height: 1.48;
    }

    .requirement-meta {
        font-size: 0.76rem;
        color: #6c7f98;
        padding-top: 0.5rem;
        border-top: 1px dashed #dde8f9;
    }

    .empty-state {
        padding: 2rem 1rem;
        text-align: center;
        color: #687a93;
    }

    .empty-state i {
        color: #99adc8;
    }

    .bulk-actions {
        background: linear-gradient(135deg, #fff9e7 0%, #fff2c7 100%);
        border: 1px solid #f4df9e;
        border-radius: 12px;
        padding: 0.8rem 1rem;
        margin-bottom: 1.2rem;
        display: none;
        box-shadow: 0 8px 18px rgba(128, 101, 34, 0.14);
    }

    .bulk-actions.active {
        display: block;
    }

    #requirementModal .modal-content,
    #deleteModal .modal-content {
        border: none;
        border-radius: 14px;
        overflow: hidden;
        box-shadow: 0 18px 34px rgba(24, 34, 53, 0.24);
    }

    #requirementModal .modal-header {
        background: linear-gradient(135deg, #1f6feb 0%, #1942a8 100%);
        color: #fff;
        border-bottom: none;
    }

    #deleteModal .modal-header {
        background: linear-gradient(135deg, #dc3545 0%, #bf2232 100%);
        color: #fff;
        border-bottom: none;
    }

    #requirementModal .close,
    #deleteModal .close {
        color: #fff;
        opacity: 0.85;
    }

    @media (max-width: 991.98px) {
        .requirements-hero {
            padding: 1.05rem;
        }

        .hero-actions {
            margin-top: 0.65rem;
        }

        .hero-actions .btn {
            width: 100%;
            margin-bottom: 0.4rem;
        }
    }

    @media (max-width: 768px) {
        .requirement-card {
            margin-bottom: 0.9rem;
        }

        .action-buttons {
            opacity: 1;
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="container-fluid requirements-shell">
    <div class="requirements-hero mb-4">
        <div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-center">
            <div>
                <h1 class="h3 mb-1">
                    <i class="fas fa-clipboard-list mr-2"></i>Manage Requirements
                </h1>
                <p class="subtitle">Create, maintain, and organize requirement rules used across assistance programs.</p>
                <div class="hero-kpis">
                    <span class="kpi-chip"><i class="fas fa-layer-group"></i>{{ stats.total }} Total</span>
                    <span class="kpi-chip"><i class="fas fa-file-alt"></i>{{ stats.document }} Documents</span>
                    <span class="kpi-chip"><i class="fas fa-user-check"></i>{{ stats.qualification }} Qualifications</span>
                </div>
            </div>
            <div class="hero-actions mt-3 mt-lg-0">
                <a href="{{ url_for('admin.adm_programs') }}" class="btn btn-secondary mr-lg-2">
                    <i class="fas fa-arrow-left mr-1"></i>Back to Programs
                </a>
                <button class="btn btn-primary" onclick="openAddRequirementModal()">
                    <i class="fas fa-plus mr-1"></i>Add New Requirement
                </button>
            </div>
        </div>
    </div>

    <!-- Statistics Cards -->
    <div class="row mb-4">
        <div class="col-md-4 col-lg mb-3">
            <div class="card stats-card border-left-secondary h-100">
                <div class="card-body text-center">
                    <div class="h4 font-weight-bold mb-1">{{ stats.total }}</div>
                    <div class="text-xs font-weight-bold text-uppercase">Total Requirements</div>
                </div>
            </div>
        </div>
        <div class="col-md-4 col-lg mb-3">
            <div class="card stats-card border-left-primary shadow h-100">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">Documents</div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">{{ stats.document }}</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-file-alt fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-4 col-lg mb-3">
            <div class="card stats-card border-left-success shadow h-100">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-success text-uppercase mb-1">Qualifications</div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">{{ stats.qualification }}</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-user-check fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6 col-lg mb-3">
            <div class="card stats-card border-left-info shadow h-100">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-info text-uppercase mb-1">In Use</div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">{{ stats.used }}</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-check-circle fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6 col-lg mb-3">
            <div class="card stats-card border-left-warning shadow h-100">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">Unused</div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">{{ stats.unused }}</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-exclamation-triangle fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Filter Section -->
    <div class="filter-section">
        <div class="row align-items-center">
            <div class="col-lg-4 col-md-6 mb-3 mb-lg-0">
                <label class="filter-label" for="searchInput">Search</label>
                <div class="input-group">
                    <div class="input-group-prepend">
                        <span class="input-group-text"><i class="fas fa-search"></i></span>
                    </div>
                    <input type="text" class="form-control" id="searchInput" placeholder="Search requirements...">
                </div>
            </div>
            <div class="col-lg-3 col-md-6 mb-3 mb-lg-0">
                <label class="filter-label" for="typeFilter">Type</label>
                <select class="form-control" id="typeFilter">
                    <option value="">All Types</option>
                    <option value="document">Document Requirements</option>
                    <option value="qualification">Qualification Requirements</option>
                </select>
            </div>
            <div class="col-lg-3 col-md-6 mb-3 mb-md-0">
                <label class="filter-label" for="usageFilter">Usage</label>
                <select class="form-control" id="usageFilter">
                    <option value="">All Requirements</option>
                    <option value="used">In Use</option>
                    <option value="unused">Unused</option>
                </select>
            </div>
            <div class="col-lg-2 col-md-6">
                <label class="filter-label d-none d-md-block">Reset</label>
                <button class="btn btn-outline-secondary btn-block filter-clear-btn" onclick="clearFilters()">
                    <i class="fas fa-undo mr-1"></i>Clear
                </button>
            </div>
        </div>
    </div>

    <!-- Bulk Actions Bar -->
    <div class="bulk-actions" id="bulkActionsBar">
        <div class="row align-items-center">
            <div class="col-md-6">
                <strong><span id="selectedCount">0</span> requirements selected</strong>
            </div>
            <div class="col-md-6 text-right">
                <button class="btn btn-sm btn-warning mr-2" onclick="bulkEdit()">
                    <i class="fas fa-edit mr-1"></i>Bulk Edit
                </button>
                <button class="btn btn-sm btn-danger" onclick="bulkDelete()">
                    <i class="fas fa-trash mr-1"></i>Delete Selected
                </button>
            </div>
        </div>
    </div>

    <!-- Document Requirements Section -->
    <div class="card section-surface-card mb-4">
        <div class="card-header section-header doc-header d-flex justify-content-between align-items-center">
            <h6 class="m-0 font-weight-bold">
                <i class="fas fa-file-alt mr-2"></i>Document Requirements ({{ document_requirements|length }})
            </h6>
            <button class="btn btn-sm btn-light section-add-btn" onclick="openAddRequirementModal('document')">
                <i class="fas fa-plus mr-1"></i>Add Document
            </button>
        </div>
        <div class="card-body">
            {% if document_requirements %}
                <div class="row" id="documentRequirements">
                    {% for req_data in document_requirements %}
                    <div class="col-lg-6 col-xl-4 mb-3 requirement-item" 
                         data-type="document" 
                         data-usage="{{ req_data.usage_count }}"
                         data-name="{{ req_data.requirement.requirement_name|lower }}">
                        <div class="requirement-card">
                            <div class="requirement-header" onclick="toggleRequirementDetails({{ req_data.requirement.id }})">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div class="flex-grow-1">
                                        <div class="requirement-name">
                                            <input type="checkbox" class="mr-2 requirement-checkbox" value="{{ req_data.requirement.id }}">
                                            <span>{{ req_data.requirement.requirement_name }}</span>
                                        </div>
                                        <div class="d-flex align-items-center">
                                            <span class="usage-badge badge badge-{% if req_data.usage_count > 0 %}info{% else %}secondary{% endif %} mr-2">
                                                <i class="fas fa-chart-bar mr-1"></i>{{ req_data.usage_count }} programs
                                            </span>
                                            {% if req_data.usage_count == 0 %}
                                            <span class="badge badge-warning">Unused</span>
                                            {% endif %}
                                        </div>
                                    </div>
                                    <div class="d-flex align-items-center ml-2">
                                        <span class="expand-cue" title="Click to expand"><i class="fas fa-chevron-down"></i></span>
                                        <div class="action-buttons">
                                        <button class="btn btn-sm btn-outline-primary" 
                                                onclick="editRequirement({{ req_data.requirement.id }}, '{{ req_data.requirement.requirement_name }}', '{{ req_data.requirement.description or '' }}', 'document'); event.stopPropagation();"
                                                title="Edit requirement">
                                            <i class="fas fa-edit"></i>
                                        </button>
                                        <button class="btn btn-sm btn-outline-danger ml-1" 
                                                onclick="deleteRequirement({{ req_data.requirement.id }}, '{{ req_data.requirement.requirement_name }}'); event.stopPropagation();"
                                                title="Delete requirement">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="requirement-body collapse" id="details-{{ req_data.requirement.id }}">
                                {% if req_data.requirement.description %}
                                <div class="requirement-description">
                                    <strong>Description:</strong><br>
                                    {{ req_data.requirement.description }}
                                </div>
                                {% endif %}
                                
                                {% if req_data.programs %}
                                <div class="mb-3">
                                    <strong class="d-block mb-2">Used in Programs:</strong>
                                    {% for prog_data in req_data.programs %}
                                    <span class="program-tag mandatory">
                                        {{ prog_data.program.program_name }}
                                        <i class="fas fa-exclamation-circle ml-1" title="Required"></i>
                                    </span>
                                    {% endfor %}
                                </div>
                                {% endif %}
                                
                                <div class="requirement-meta">
                                    <small class="text-muted">
                                        <i class="fas fa-tag mr-1"></i>Type: Document Requirement
                                        {% if req_data.usage_count > 0 %}
                                        | <i class="fas fa-link mr-1"></i>{{ req_data.usage_count }} program(s)
                                        {% endif %}
                                    </small>
                                </div>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            {% else %}
                <div class="empty-state">
                    <i class="fas fa-file-alt fa-3x mb-3"></i>
                    <p class="mb-2">No document requirements found</p>
                    <button class="btn btn-outline-primary" onclick="openAddRequirementModal('document')">
                        <i class="fas fa-plus mr-1"></i>Add First Document Requirement
                    </button>
                </div>
            {% endif %}
        </div>
    </div>

    <!-- Qualification Requirements Section -->
    <div class="card section-surface-card mb-4">
        <div class="card-header section-header qual-header d-flex justify-content-between align-items-center">
            <h6 class="m-0 font-weight-bold">
                <i class="fas fa-user-check mr-2"></i>Qualification Requirements ({{ qualification_requirements|length }})
            </h6>
            <button class="btn btn-sm btn-light section-add-btn" onclick="openAddRequirementModal('qualification')">
                <i class="fas fa-plus mr-1"></i>Add Qualification
            </button>
        </div>
        <div class="card-body">
            {% if qualification_requirements %}
                <div class="row" id="qualificationRequirements">
                    {% for req_data in qualification_requirements %}
                    <div class="col-lg-6 col-xl-4 mb-3 requirement-item" 
                         data-type="qualification" 
                         data-usage="{{ req_data.usage_count }}"
                         data-name="{{ req_data.requirement.requirement_name|lower }}">
                        <div class="requirement-card">
                            <div class="requirement-header" onclick="toggleRequirementDetails({{ req_data.requirement.id }})">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div class="flex-grow-1">
                                        <div class="requirement-name">
                                            <input type="checkbox" class="mr-2 requirement-checkbox" value="{{ req_data.requirement.id }}">
                                            <span>{{ req_data.requirement.requirement_name }}</span>
                                        </div>
                                        <div class="d-flex align-items-center">
                                            <span class="usage-badge badge badge-{% if req_data.usage_count > 0 %}success{% else %}secondary{% endif %} mr-2">
                                                <i class="fas fa-chart-bar mr-1"></i>{{ req_data.usage_count }} programs
                                            </span>
                                            {% if req_data.usage_count == 0 %}
                                            <span class="badge badge-warning">Unused</span>
                                            {% endif %}
                                        </div>
                                    </div>
                                    <div class="d-flex align-items-center ml-2">
                                        <span class="expand-cue" title="Click to expand"><i class="fas fa-chevron-down"></i></span>
                                        <div class="action-buttons">
                                        <button class="btn btn-sm btn-outline-success" 
                                                onclick="editRequirement({{ req_data.requirement.id }}, '{{ req_data.requirement.requirement_name }}', '{{ req_data.requirement.description or '' }}', 'qualification'); event.stopPropagation();"
                                                title="Edit requirement">
                                            <i class="fas fa-edit"></i>
                                        </button>
                                        <button class="btn btn-sm btn-outline-danger ml-1" 
                                                onclick="deleteRequirement({{ req_data.requirement.id }}, '{{ req_data.requirement.requirement_name }}'); event.stopPropagation();"
                                                title="Delete requirement">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="requirement-body collapse" id="details-{{ req_data.requirement.id }}">
                                {% if req_data.requirement.description %}
                                <div class="requirement-description">
                                    <strong>Description:</strong><br>
                                    {{ req_data.requirement.description }}
                                </div>
                                {% endif %}
                                
                                {% if req_data.programs %}
                                <div class="mb-3">
                                    <strong class="d-block mb-2">Used in Programs:</strong>
                                    {% for prog_data in req_data.programs %}
                                    <span class="program-tag mandatory">
                                        {{ prog_data.program.program_name }}
                                        <i class="fas fa-exclamation-circle ml-1" title="Required"></i>
                                    </span>
                                    {% endfor %}
                                </div>
                                {% endif %}
                                
                                <div class="requirement-meta">
                                    <small class="text-muted">
                                        <i class="fas fa-tag mr-1"></i>Type: Qualification Requirement
                                        {% if req_data.usage_count > 0 %}
                                        | <i class="fas fa-link mr-1"></i>{{ req_data.usage_count }} program(s)
                                        {% endif %}
                                    </small>
                                </div>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            {% else %}
                <div class="empty-state">
                    <i class="fas fa-user-check fa-3x mb-3"></i>
                    <p class="mb-2">No qualification requirements found</p>
                    <button class="btn btn-outline-success" onclick="openAddRequirementModal('qualification')">
                        <i class="fas fa-plus mr-1"></i>Add First Qualification Requirement
                    </button>
                </div>
            {% endif %}
        </div>
    </div>
</div>

<!-- Add/Edit Requirement Modal -->
<div class="modal fade" id="requirementModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="requirementModalTitle">Add New Requirement</h5>
                <button type="button" class="close" data-dismiss="modal">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form id="requirementForm">
                <div class="modal-body">
                    <input type="hidden" id="requirementId" name="requirement_id">
                    <input type="hidden" id="isEdit" value="false">
                    
                    <div class="form-group">
                        <label>Requirement Type <span class="text-danger">*</span></label>
                        <select class="form-control" id="requirementType" name="requirement_type" required>
                            <option value="document">Document Requirement</option>
                            <option value="qualification">Qualification Requirement</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label>Requirement Name <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" id="requirementName" name="requirement_name" required
                               placeholder="e.g., Valid ID, Low Income Family">
                    </div>
                    
                    <div class="form-group">
                        <label>Description</label>
                        <textarea class="form-control" id="requirementDescription" name="description" rows="3"
                                  placeholder="Optional description to help users understand this requirement"></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">
                        <i class="fas fa-times mr-1"></i>Cancel
                    </button>
                    <button type="submit" class="btn btn-primary" id="saveRequirementBtn">
                        <i class="fas fa-save mr-1"></i>Save Requirement
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Delete Requirement</h5>
                <button type="button" class="close" data-dismiss="modal">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete the requirement "<strong id="deleteRequirementName"></strong>"?</p>
                <p class="text-muted small">This action cannot be undone and will also remove all links to programs and related application requirement records.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">
                    <i class="fas fa-times mr-1"></i>Cancel
                </button>
                <button type="button" class="btn btn-danger" id="confirmDeleteBtn">
                    <i class="fas fa-trash mr-1"></i>Delete Requirement
                </button>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block extra_js %}
<script>
let selectedRequirements = [];

// Initialize page functionality
$(document).ready(function() {
    initializeFilters();
    initializeCheckboxes();
    initializeModals();
});

// Filter functionality
function initializeFilters() {
    const searchInput = document.getElementById('searchInput');
    const typeFilter = document.getElementById('typeFilter');
    const usageFilter = document.getElementById('usageFilter');
    
    searchInput.addEventListener('input', applyFilters);
    typeFilter.addEventListener('change', applyFilters);
    usageFilter.addEventListener('change', applyFilters);
}

function applyFilters() {
    const searchTerm = document.getElementById('searchInput').value.toLowerCase();
    const typeFilter = document.getElementById('typeFilter').value;
    const usageFilter = document.getElementById('usageFilter').value;
    
    const items = document.querySelectorAll('.requirement-item');
    
    items.forEach(item => {
        const name = item.dataset.name;
        const type = item.dataset.type;
        const usage = parseInt(item.dataset.usage);
        
        let showItem = true;
        
        // Apply search filter
        if (searchTerm && !name.includes(searchTerm)) {
            showItem = false;
        }
        
        // Apply type filter
        if (typeFilter && type !== typeFilter) {
            showItem = false;
        }
        
        // Apply usage filter
        if (usageFilter === 'used' && usage === 0) {
            showItem = false;
        } else if (usageFilter === 'unused' && usage > 0) {
            showItem = false;
        }
        
        item.style.display = showItem ? '' : 'none';
    });
}

function clearFilters() {
    document.getElementById('searchInput').value = '';
    document.getElementById('typeFilter').value = '';
    document.getElementById('usageFilter').value = '';
    applyFilters();
}

// Checkbox functionality
function initializeCheckboxes() {
    document.addEventListener('change', function(e) {
        if (e.target.classList.contains('requirement-checkbox')) {
            updateSelectedRequirements();
        }
    });
}

function updateSelectedRequirements() {
    const checkboxes = document.querySelectorAll('.requirement-checkbox:checked');
    selectedRequirements = Array.from(checkboxes).map(cb => parseInt(cb.value));
    
    const bulkActionsBar = document.getElementById('bulkActionsBar');
    const selectedCount = document.getElementById('selectedCount');
    
    if (selectedRequirements.length > 0) {
        bulkActionsBar.classList.add('active');
        selectedCount.textContent = selectedRequirements.length;
    } else {
        bulkActionsBar.classList.remove('active');
    }
}

// Modal functionality
function initializeModals() {
    document.getElementById('requirementForm').addEventListener('submit', handleRequirementSubmit);
}

function openAddRequirementModal(type = 'document') {
    document.getElementById('requirementModalTitle').textContent = 'Add New Requirement';
    document.getElementById('requirementId').value = '';
    document.getElementById('isEdit').value = 'false';
    document.getElementById('requirementType').value = type;
    document.getElementById('requirementName').value = '';
    document.getElementById('requirementDescription').value = '';
    document.getElementById('saveRequirementBtn').innerHTML = '<i class="fas fa-save mr-1"></i>Save Requirement';
    
    $('#requirementModal').modal('show');
}

function editRequirement(id, name, description, type) {
    document.getElementById('requirementModalTitle').textContent = 'Edit Requirement';
    document.getElementById('requirementId').value = id;
    document.getElementById('isEdit').value = 'true';
    document.getElementById('requirementType').value = type;
    document.getElementById('requirementName').value = name;
    document.getElementById('requirementDescription').value = description;
    document.getElementById('saveRequirementBtn').innerHTML = '<i class="fas fa-save mr-1"></i>Update Requirement';
    
    $('#requirementModal').modal('show');
}

function handleRequirementSubmit(e) {
    e.preventDefault();
    
    const isEdit = document.getElementById('isEdit').value === 'true';
    const formData = new FormData(document.getElementById('requirementForm'));
    const data = Object.fromEntries(formData.entries());
    
    const saveBtn = document.getElementById('saveRequirementBtn');
    const originalText = saveBtn.innerHTML;
    saveBtn.disabled = true;
    saveBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-1"></i>Saving...';
    
    const url = isEdit 
        ? `{{ url_for('admin.edit_requirement_ajax', requirement_id=0) }}`.replace('0', data.requirement_id)
        : '{{ url_for("admin.add_requirement_ajax") }}';
    
    const method = isEdit ? 'PUT' : 'POST';
    
    fetch(url, {
        method: method,
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify(data)
    })
    .then(response => response.json())
    .then(result => {
        if (result.success) {
            showToast(result.message || 'Requirement saved successfully!', 'success');
            $('#requirementModal').modal('hide');
            setTimeout(() => window.location.reload(), 1000);
        } else {
            showToast(result.message || 'Error saving requirement', 'danger');
        }
    })
    .catch(error => {
        console.error('Error:', error);
        showToast('Error saving requirement', 'danger');
    })
    .finally(() => {
        saveBtn.disabled = false;
        saveBtn.innerHTML = originalText;
    });
}

function deleteRequirement(id, name) {
    document.getElementById('deleteRequirementName').textContent = name;
    
    document.getElementById('confirmDeleteBtn').onclick = function() {
        const btn = this;
        const originalText = btn.innerHTML;
        btn.disabled = true;
        btn.innerHTML = '<i class="fas fa-spinner fa-spin mr-1"></i>Deleting...';
        
        fetch(`{{ url_for('admin.delete_requirement_ajax', requirement_id=0) }}`.replace('0', id), {
            method: 'DELETE'
        })
        .then(response => response.json())
        .then(result => {
            if (result.success) {
                showToast(result.message || 'Requirement deleted successfully!', 'success');
                $('#deleteModal').modal('hide');
                setTimeout(() => window.location.reload(), 1000);
            } else {
                showToast(result.message || 'Error deleting requirement', 'danger');
            }
        })
        .catch(error => {
            console.error('Error:', error);
            showToast('Error deleting requirement', 'danger');
        })
        .finally(() => {
            btn.disabled = false;
            btn.innerHTML = originalText;
        });
    };
    
    $('#deleteModal').modal('show');
}

// Requirement details toggle
function toggleRequirementDetails(id) {
    const details = document.getElementById(`details-${id}`);
    $(details).collapse('toggle');
}

// Bulk actions (placeholder functions)
function bulkEdit() {
    if (selectedRequirements.length === 0) {
        showToast('Please select requirements to edit', 'warning');
        return;
    }
    
    // Implement bulk edit functionality
    showToast('Bulk edit functionality coming soon', 'info');
}

function bulkDelete() {
    if (selectedRequirements.length === 0) {
        showToast('Please select requirements to delete', 'warning');
        return;
    }
    
    // Implement bulk delete functionality
    showToast('Bulk delete functionality coming soon', 'info');
}

// Toast notification function
function showToast(message, type = 'info') {
    let toastContainer = document.getElementById('ayudaToastStackContainer');
    if (!toastContainer) {
        toastContainer = document.createElement('div');
        toastContainer.id = 'ayudaToastStackContainer';
        toastContainer.style.cssText = 'position: fixed; top: 20px; right: 20px; z-index: 9999; display: flex; flex-direction: column; gap: 10px; align-items: flex-end; width: min(92vw, 400px); pointer-events: none;';
        document.body.appendChild(toastContainer);
    }

    // Create toast element
    const toast = document.createElement('div');
    toast.className = `alert alert-${type} alert-dismissible fade show mb-0 toast-animate-in`;
    toast.style.cssText = 'width: 400px; max-width: calc(100vw - 1rem); box-shadow: 0 8px 20px rgba(0,0,0,0.18); pointer-events: auto;';
    toast.innerHTML = `
        ${message}
        <button type="button" class="close" data-dismiss="alert">
            <span aria-hidden="true">&times;</span>
        </button>
    `;
    
    toastContainer.appendChild(toast);
    
    // Auto remove after 5 seconds
    setTimeout(() => {
        if (toast.parentNode) {
            toast.classList.remove('toast-animate-in');
            toast.classList.add('toast-animate-out');
            setTimeout(() => {
                if (toast.parentNode) {
                    toast.remove();
                }
            }, 300);
        }
    }, 5000);
}
</script>
{% endblock %}